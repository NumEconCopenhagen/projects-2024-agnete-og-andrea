# Data analysis project

Our project is titled **DataProject: Population Projection in Denmark** and is about how the Danish population developes.

The **results** of the project can be seen from running [dataproject.ipynb](dataproject.ipynb).

We download a dataset from DST via the DSTAPI.

**Dependencies:** Apart from a standard Anaconda Python 3 installation, the project requires the following installations:

``%pip install git+https://github.com/alemartinello/dstapi``

This is also automatically installed when running the dataproject.ipynb file.
